// const { 
//     Commands,
//     Session,
//     Commander
// } = require('@carmel/sdk')

export default function exec (args: any) {
    console.log(args)
    // const homeDir = process.cwd()
    // const env = { homeDir }    
    // const cmd = Object.assign({}, { args, env }, { id: args._[0] } )
    // const cls = cmd.id.charAt(0).toUpperCase() + cmd.id.slice(1)
    // const Command = Commands[cls]
    
    // const command = new Command(cmd)
    // const session = new Session({ dir: homeDir })

    // Commander.run(command, session)
}