it('change the title to a new one', () => {
  const title = 'dd'
  expect(title).to.equal('dd', 'Hey make sure you do this right')
  // expect(title).to.equal('dd3', 'Hey make sure you do this right')
})
